# Meta

env: "production"
docs_file: "website.min"

# Build
include:
  hide_carbon: true

markdown: kramdown
permalink: pretty
url: /bulma
exclude:
  [
    ".babelrc",
    "docker-compose.yml",
    "icons",
    "node_modules",
    "package.json",
    "templates",
    "website.scss",
    "yarn.lock",
  ]
